# Supabase 클라우드 백업 (선택). 없으면 앱은 로컬(localStorage)로만 동작해요.
# 아래 두 값을 채우고 파일명을 .env.local 로 바꾼 뒤 `npm run dev` 재시작하면 클라우드 동기화가 켜져요.
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR-ANON-PUBLIC-KEY
